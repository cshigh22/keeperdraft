# Database
DATABASE_URL="postgresql://username:password@localhost:5432/keeperdraft?schema=public"

# Next.js / Auth.js (v5)
AUTH_SECRET="your-auth-secret-here" # Generate with: npx auth secret
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Google OAuth
AUTH_GOOGLE_ID="your-google-client-id"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# Socket.IO
# The socket server also needs DATABASE_URL and AUTH_SECRET. AUTH_SECRET must be
# the same value as the Next.js app uses, or no client can authenticate.
SOCKET_PORT="3001"
NEXT_PUBLIC_SOCKET_URL="http://localhost:3001"

# Environment
NODE_ENV="development"
